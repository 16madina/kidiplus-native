# Minimal config for `supabase functions deploy`.
# Stripe calls the webhook without a Supabase JWT.

[functions.connect-webhook]
verify_jwt = false

[functions.connect-bounce]
verify_jwt = false
# Stripe redirects the vendor's Safari here — no JWT.
